add_namespace = pf_super_event

# Run `event pf_super_event.1` from the debug console to verify the
# installed GUI, GFX, scripted GUI, scripted localisation, and close action.
country_event = {
    id = pf_super_event.1
    hidden = yes
    is_triggered_only = yes

    immediate = {
        pf_show_example_super_event = yes
    }
}
